(function ($) {
  "use strict";

  // Toggle main category dropdown
  // $('body').on('click', '.xs-categories-toggle', function (e) {
  //   if (e.target !== this) return;

  //   if ($(this).hasClass('show-items')) {
  //     $(this).removeClass('show-items');
  //   } else {
  //     $('.xs-categories-toggle').removeClass('show-items'); // Close others
  //     $(this).addClass('show-items');
  //   }
  // });

  // Close on click outside
  // $('body').on('click', function (event) {
  //   if (!$(event.target).closest('.xs-categories-toggle').length) {
  //     $(".xs-categories-toggle").removeClass('show-items');
  //   }
  // });

  // Toggle subcategory menu
  // $('body').on('click', '.cat-dropdown-menu > li > a.js-has-subcategory', function (e) {
  //   e.preventDefault();
  //   const $parent = $(this).closest('li');
  //   const isOpen = $parent.hasClass('show-sub-menu');

  //   $('.cat-dropdown-menu > li').removeClass('show-sub-menu');
  //   if (!isOpen) {
  //     $parent.addClass('show-sub-menu');
  //   }
  // });

  

  // Navbar toggle (unchanged)
  $('body').on('click', '#navbarClose', function (e) {
    $('#navbarContent').removeClass('show');
  });
  $('body').on('click', '#navbarToggle', function (e) {
    $('#navbarContent').addClass('show');
  });

  // Sticky navbar
  var navbar = document.getElementById("navbar");
  var navbarVacuum = document.getElementById("navbarVacuum");
  var sticky = navbar.offsetTop;

  function myFunction() {
    if (window.pageYOffset >= sticky) {
      navbarVacuum.style.height = navbar.offsetHeight + 'px';
      navbar.classList.add("sticky");
    } else {
      navbarVacuum.style.height = 0;
      navbar.classList.remove("sticky");
    }
  }

  window.onscroll = function () {
    myFunction();
  };
})(jQuery);
